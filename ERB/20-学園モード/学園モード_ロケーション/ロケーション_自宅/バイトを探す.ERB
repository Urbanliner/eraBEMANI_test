@バイトを探す
#DIM DYNAMIC LCOUNT
#DIM DYNAMIC LVARLINE
#DIM DYNAMIC 面接官

DRAWLINE
LVARLINE = LINECOUNT
FOR LCOUNT, 0, ロケーション数
	TRYCCALLFORM バイト条件_%SHORTLOCATIONRPS(ロケーション:LCOUNT)%
		SIF RESULT && バイト先 != SHORTLOCATION(ロケーション:LCOUNT)
			PRINTFORML [{LCOUNT, 2}] - %SHORTLOCATION(ロケーション:LCOUNT)% %バイト要項%
	CATCH
	ENDCATCH
NEXT
IF LVARLINE == LINECOUNT
	PRINTFORML 
	PRINTFORML -----求人がありません-----
	PRINTFORML 
ENDIF
DRAWLINE
PRINTPLAIN 　求人一覧　
PRINTL [-1] 戻る

BINPUT

SIF RESULT == -1
	RETURN 0
応募先 = %SHORTLOCATION(ロケーション:RESULT)%
CALLFORM バイト条件_%RPS(応募先)%
面接官 = RESULT

DRAWLINE
IF 面接官 > 0
	CALL SCHOOL_KOJO, 面接官, "面接"
ELSE
	PRINTFORMW %応募先%のバイトの面接に行った
	PRINTFORMW 面接の結果、採用が決定した！
ENDIF

バイト先 = %応募先%

RETURN 1

@SCHOOL_MESSAGE_面接
PRINTFORMW %応募先%のバイトの面接に行った
PRINTFORMW 面接を行った%CALLNAME:TARGET%は%CALLNAME:PLAYER%を採用してくれた！
